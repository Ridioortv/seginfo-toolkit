# --- General ---
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- Postgres ---
POSTGRES_USER=sentinelops
POSTGRES_PASSWORD=changeme
POSTGRES_DB=sentinelops
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Auth service ---
JWT_SECRET_KEY=changeme-generate-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
MFA_ISSUER_NAME=SentinelOps

# --- Asset / Scan / Vuln services ---
# GVM_SOCKET_PATH y GVM_USER son opcionales: solo se usan si se habilita el
# driver OpenVAS en scan-service (ver backend/services/scan-service/app/scanners/openvas.py)
GVM_SOCKET_PATH=
GVM_USER=
EPSS_API_URL=https://api.first.org/data/v1/epss
CISA_KEV_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:8001
VITE_ASSET_API_BASE_URL=http://localhost:8002
VITE_SCAN_API_BASE_URL=http://localhost:8003
VITE_VULN_API_BASE_URL=http://localhost:8004
